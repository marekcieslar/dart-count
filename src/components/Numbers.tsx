type NumbersProps = {
	// Later we can add props here
};

function Numbers(_props: NumbersProps) {
	return (
		<section>
			<h1>Numbers</h1>
			<p>Here you will work with scores and simple inputs.</p>
		</section>
	);
}

export default Numbers;
